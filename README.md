# numerical-linalg-impl
Some algorithm implementations in python.

`.env_example` contains a variable named `IMG_PATH`, which is a file path for the functions in `image_compression.py`. To compress images, create a file called `.env` and paste the appropriate path there.